# AeroDeck 7.0 Framework Overhaul Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use aerodeck:subagent-driven-task-pipeline (recommended) or aerodeck:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transform AeroDeck into a state-of-the-art general agentic framework for Antigravity 2.0 / IDE / CLI with adaptive execution fast-tracking, resilient model router cascades, background scheduling workflows, and a 5-tier QA pyramid.

**Architecture/Workflow:** Operational Configuration B. 4-stage execution covering Infrastructure Fixes, Skill Engine Optimization, Antigravity 2.0 Advanced Workflows, and Model Router/QA Pyramid Enhancement.

**Tech Stack/Tools:** Node.js 18+, TypeScript, Vercel AI SDK (`ai`), PowerShell, POSIX Bash, Jest, `@modelcontextprotocol/sdk`.

---

### Task 1: Infrastructure & Installation Script Remediation

**Targets:**
- Modify: `uninstall.ps1:15-25`
- Modify: `install.ps1:20-35`
- Modify: `plugin.json`
- Modify: `gemini-extension.json:1-10`
- Modify: `skills/using-aerodeck/references/antigravity-tools.md`

- [ ] **Step 1: Define success criteria**
  - `uninstall.ps1` removes `google-drive` from `mcp_config.json` alongside `browser-automation` and `model-router`.
  - `install.ps1` copies `mcp-servers/` to `$pluginDir` and registers path `$pluginDir/mcp-servers/...` in `mcp_config.json`.
  - `plugin.json` specifies version `7.0.0`, skill entry points, and MCP tool dependencies.
  - Legacy superpowers `code-reviewer` references removed from reference guides.

- [ ] **Step 2: Verify current state fails/lacks criteria**
  - Check `uninstall.ps1`: missing `google-drive`.
  - Check `install.ps1`: registers `$cwd`.
  - Expected: FAIL.

- [ ] **Step 3: Perform implementation**
  - Edit `uninstall.ps1` to include `$mcpConfig.mcpServers.PSObject.Properties.Remove("google-drive")`.
  - Edit `install.ps1` to copy `mcp-servers` directory into `$pluginDir/mcp-servers/` and set `$browserServerPath = Join-Path $pluginDir "mcp-servers\browser-automation\dist\src\index.js"`.
  - Edit `plugin.json` to enrich manifest to v7.0.0.
  - Edit `gemini-extension.json` and `skills/using-aerodeck/references/antigravity-tools.md` to remove legacy superpowers references.

- [ ] **Step 4: Verify state passes criteria**
  - Validate PowerShell script syntax: `powershell -Command "Get-Command .\install.ps1"`
  - Verify JSON files parse cleanly: `node -e "JSON.parse(fs.readFileSync('plugin.json'))"`
  - Expected: PASS.

- [ ] **Step 5: Save/Checkpoint**
  - Commit changes to git.

---

### Task 2: Skill Engine & Adaptive Fast-Track Integration

**Targets:**
- Modify: `skills/using-aerodeck/SKILL.md`
- Modify: `skills/brainstorming/SKILL.md`
- Create: `skills/background-task-scheduling/SKILL.md`
- Create: `skills/resilient-model-fallback/SKILL.md`

- [ ] **Step 1: Define success criteria**
  - `using-aerodeck/SKILL.md` contains an **Adaptive Fast-Track Complexity Gate** directing minor tasks directly to `criteria-driven-refinement`.
  - `brainstorming/SKILL.md` includes explicit fast-track bypass rule for single-step requests.
  - `background-task-scheduling/SKILL.md` defines instructions for `schedule` tool (one-shot, cron, `TimerCondition`, `IsDaemon`).
  - `resilient-model-fallback/SKILL.md` defines fallbacks when MCP servers are offline.

- [ ] **Step 2: Verify current state fails/lacks criteria**
  - Check `skills/background-task-scheduling/SKILL.md` existence.
  - Expected: FAIL (File missing).

- [ ] **Step 3: Perform implementation**
  - Add Adaptive Fast-Track section in `skills/using-aerodeck/SKILL.md`.
  - Add complexity gate to `skills/brainstorming/SKILL.md`.
  - Create `skills/background-task-scheduling/SKILL.md` with YAML frontmatter and operational rules for Antigravity 2.0 `schedule` tool.
  - Create `skills/resilient-model-fallback/SKILL.md` with fallback procedures.

- [ ] **Step 4: Verify state passes criteria**
  - Verify all markdown skill files contain valid YAML frontmatter (`name`, `description`).
  - Expected: PASS.

- [ ] **Step 5: Save/Checkpoint**
  - Commit changes to git.

---

### Task 3: MCP Model Router Resiliency & Failover Enhancement

**Targets:**
- Modify: `mcp-servers/model-router/config.json`
- Modify: `mcp-servers/model-router/src/index.ts`
- Modify: `mcp-servers/model-router/src/session.ts`

- [ ] **Step 1: Define success criteria**
  - `config.json` contains `fallbacks` arrays per tier profile.
  - `routeTask` in `src/index.ts` catches provider errors (429, 500, timeout) and cascades to fallback providers before throwing.
  - `routeTask` passes structured message arrays to Vercel AI SDK.
  - `session.ts` handles atomic file writes to prevent race conditions during concurrent subagent calls.

- [ ] **Step 2: Verify current state fails/lacks criteria**
  - Run existing unit test: `npm --prefix mcp-servers/model-router test`
  - Inspect `routeTask` for fallback loop. Expected: FAIL (No fallback loop).

- [ ] **Step 3: Perform implementation**
  - Update `config.json` schema to include `fallbacks` arrays.
  - Refactor `src/index.ts` to implement try/catch fallback iterations across provider configs.
  - Refactor `src/session.ts` to use atomic write logic.

- [ ] **Step 4: Verify state passes criteria**
  - Run model router tests: `npm --prefix mcp-servers/model-router test`
  - Expected: PASS.

- [ ] **Step 5: Save/Checkpoint**
  - Rebuild model router: `npm --prefix mcp-servers/model-router run build`

---

### Task 4: Master Test Runner & Systems Reliability Expansion

**Targets:**
- Modify: `scripts/test-runner.js`
- Modify: `scripts/test-all.ps1`

- [ ] **Step 1: Define success criteria**
  - `scripts/test-runner.js` executes `ws-protocol.test.js`, `server.test.js`, and `test-tool-mapping-accuracy.sh`.
  - `scripts/test-runner.js` dynamically selects `python3` or `python` based on system environment.
  - `scripts/test-all.ps1` passes all offline unit tests cleanly on Windows PowerShell.

- [ ] **Step 2: Verify current state fails/lacks criteria**
  - Run master test runner: `node scripts/test-runner.js`
  - Check missing tests in output. Expected: FAIL (Server & protocol tests missing).

- [ ] **Step 3: Perform implementation**
  - Update `testTargets` array in `scripts/test-runner.js` to include brainstorm server unit tests and tool mapping contract check.
  - Add python binary detector function `getPythonCommand()`.
  - Update `scripts/test-all.ps1` to execute all tests under PowerShell.

- [ ] **Step 4: Verify state passes criteria**
  - Run master test runner: `node scripts/test-runner.js`
  - Verify all test suites execute and report PASS.

- [ ] **Step 5: Save/Checkpoint**
  - Execute full test suite and tag release v7.0.0.
