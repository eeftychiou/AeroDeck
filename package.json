{
  "name": "aerodeck",
  "version": "6.0.0",
  "type": "module",
  "scripts": {
    "setup": "npm --prefix scripts/setup install && npm --prefix scripts/setup run start",
    "setup:models": "npm --prefix scripts/setup install && npm --prefix scripts/setup run setup:models",
    "setup:google": "npm --prefix scripts/setup install && npm --prefix scripts/setup run setup:google",
    "test": "node scripts/test-runner.js --unit",
    "test:unit": "node scripts/test-runner.js --unit",
    "test:live": "node scripts/test-runner.js --live"
  }
}
